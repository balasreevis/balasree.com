{
  "name": "balasree.com",
  "version": "1.0.0",
  "description": "Portfolio site for balasree.com",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "eleventy --serve --port=8080",
    "build": "eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-rss": "^3.0.0"
  }
}
